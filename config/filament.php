<?php

return [
    // Branding displayed in the topbar / header and in the sidebar
    'brand' => [
        // Visible brand name in the Filament header
        'name' => 'Sistema Agarcorp ERP',

        // Optional logo path (leave null to use text only)
        'logo' => null,

        // Optional favicon
        'favicon' => null,
    ],

    // Keep other defaults minimal - you can publish the full config if you need more options
];
